- name: "Deploy Assets Symlink"
  ansible.builtin.include_tasks: "{{ playbook_dir }}/tasks/deploy_config.yml"
  vars:
    app_name: Assets
    dest_path: "{{ ansible_facts['user_dir'] }}/.config/assets"
