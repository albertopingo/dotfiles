---
- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | [AUR] Install OBS Studio"
  kewlfft.aur.aur:
    use: "{{ aur_helper }}"
    name: obs-studio-git
    state: present
  become: true
  become_user: aur_builder

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Install OBS Studio dependencies"
  community.general.pacman:
    name:
      - v4l2loopback-dkms
    state: present
  become: true
