---
# -------------------------------
#   Pacman
# -------------------------------

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Pacman Config - Uncomment Color option"
  ansible.builtin.replace:
    path: /etc/pacman.conf
    regexp: '^#Color$'
    replace: 'Color'
    backup: yes
  become: true

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Pacman Config - Uncomment VerbosePkgLists option"
  ansible.builtin.replace:
    path: /etc/pacman.conf
    regexp: '^#VerbosePkgLists$'
    replace: 'VerbosePkgLists'
  become: true

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Pacman Config - Add ILoveCandy option"
  ansible.builtin.lineinfile:
    path: /etc/pacman.conf
    insertafter: '^Color$'
    line: 'ILoveCandy'
    state: present
  become: true

# -------------------------------
#   Flatpak
# -------------------------------
- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Install Flatpak"
  community.general.pacman:
    name:
      - flatpak
    state: present
  become: true

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Add the Flathub repository"
  community.general.flatpak_remote:
    name: flathub
    state: present
    flatpakrepo_url: https://dl.flathub.org/repo/flathub.flatpakrepo
    method: "{{ item }}"
  become: "{{ true if item == 'system' else false }}"
  loop:
    - system
    - user

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Add Flathub Beta repository"
  community.general.flatpak_remote:
    name: flathub-beta
    state: present
    flatpakrepo_url: https://flathub.org/beta-repo/flathub-beta.flatpakrepo
    method: "{{ item }}"
  become: "{{ true if item == 'system' else false }}"
  loop:
    - system
    - user

- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | Update flatpak application database"
  ansible.builtin.command:
    cmd: flatpak update --appstream