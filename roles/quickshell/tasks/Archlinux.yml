---
- name: "{{ role_name | capitalize }} | {{ ansible_distribution }} | [AUR] Install Quickshell"
  kewlfft.aur.aur:
    use: "{{ aur_helper }}"
    name: quickshell
    state: present
  become: true
  become_user: aur_builder
